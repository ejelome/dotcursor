# Cursor Migration Fixtures

Fixtures for `tools/cursor/check-cursor-migration.py` tests may be added here when a case needs reusable files. Current tests build temporary repositories inline because the fixture shape is small.
